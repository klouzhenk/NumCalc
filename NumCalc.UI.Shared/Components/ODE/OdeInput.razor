@using NumCalc.Shared.Enums.ODE
@using NumCalc.UI.Shared.Components

<div class="input-section__wrapper input-section__wrapper--ode-function">
    <Tooltip>
        <Content>
            <MathInput Label="@Localizer["OdeFunctionLabel"]" @ref="_mathInput" />
        </Content>
        <Hint>@Localizer["Hint_OdeFunction"]</Hint>
    </Tooltip>
    
    <Tooltip>
        <Content>
            <div class="input-group">
                <label class="input-group__label">x₀</label>
                <input type="number" step="any" class="input-group__input" @bind="_initialX"/>
            </div>
        </Content>
        <Hint>@Localizer["Hint_InitialX"]</Hint>
    </Tooltip>
    <Tooltip>
        <Content>
            <div class="input-group">
                <label class="input-group__label">y₀</label>
                <input type="number" step="any" class="input-group__input" @bind="_initialY"/>
            </div>
        </Content>
        <Hint>@Localizer["Hint_InitialY"]</Hint>
    </Tooltip>
</div>

<div class="input-section__wrapper">
    <Tooltip>
        <Content>
            <div class="input-group">
                <label class="input-group__label">@Localizer["TargetX"]</label>
                <input type="number" step="any" class="input-group__input" @bind="_targetX" />
            </div>
        </Content>
        <Hint>@Localizer["Hint_TargetX"]</Hint>
    </Tooltip>
    @if (Method is OdeMethod.Picard)
    {
        <Tooltip>
            <Content>
                <Dropdown TItem="int"
                          CssClass="dropdown--picard-order"
                          Label="@($"{Localizer["ApproximationOrder"]} (k)")"
                          Data="@(Enumerable.Range(1, 10))"
                          TextField="@(i => i.ToString())"
                          @bind-Value="_picardOrder" />
            </Content>
            <Hint>@Localizer["Hint_PicardOrder"]</Hint>
        </Tooltip>
    }
    <Tooltip>
        <Content>
            <div class="input-group">
                <label class="input-group__label">@Localizer["StepSize"] (h)</label>
                <input type="number" step="any" min="0.0001" class="input-group__input" @bind="_stepSize" />
            </div>
        </Content>
        <Hint>@Localizer["Hint_OdeStep"] @ComputedStepCount</Hint>
    </Tooltip>
</div>
<div class="node-table @(ShowY ? "node-table--with-y" : "")">
    <div class="node-table__header">
        <span>i</span>
        <span>x<sub>i</sub></span>
        @if (ShowY)
        {
            <span>y<sub>i</sub></span>
        }
        <button class="node-table__add-btn" type="button" @onclick="AddNode">+</button>
    </div>

    @for (var i = 0; i < _nodeCount; i++)
    {
        var index = i;
        <div class="node-table__row">
            <span>@(index + 1)</span>
            <input type="number" step="any"
                   value="@_xNodes[index]"
                   @onchange="e => { if (double.TryParse(e.Value?.ToString(), out var v)) _xNodes[index] = v; }" />
            @if (ShowY)
            {
                <input type="number" step="any"
                       value="@_yValues[index]"
                       @onchange="e => { if (double.TryParse(e.Value?.ToString(), out var v)) _yValues[index] = v; }" />
            }
            <button class="node-table__remove-btn" type="button"
                    disabled="@(_nodeCount <= 2)"
                    @onclick="() => RemoveNode(index)">&times;</button>
        </div>
    }
</div>

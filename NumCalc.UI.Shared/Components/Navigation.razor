<nav class="nav-menu">
    
    <div class="nav-group">
        <div class="nav-header">Algebra</div>
        <NavLink href="analysis/roots" class="nav-item" Match="NavLinkMatch.Prefix">
            <span class="icon">🌱</span> Roots Finding
        </NavLink>
        <NavLink href="systems" class="nav-item">
            <span class="icon">🔢</span> Linear Systems
        </NavLink>
    </div>

    <div class="nav-group">
        <div class="nav-header">Calculus</div>
        <NavLink href="integration" class="nav-item">
            <span class="icon">∫</span> Integration
        </NavLink>
        <NavLink href="differentiation" class="nav-item">
            <span class="icon">∂</span> Differentiation
        </NavLink>
    </div>
    
</nav>

@code {
    [Parameter] public EventCallback OnLinkClick { get; set; }
}

<style>
    .nav-menu { padding: 10px 0; }
    
    .nav-header {
        padding: 15px 20px 5px;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #95a5a6;
        font-weight: bold;
    }

    .nav-item {
        display: flex;
        align-items: center;
        padding: 12px 20px;
        color: #ecf0f1;
        text-decoration: none;
        transition: background 0.2s;
        border-left: 4px solid transparent;
    }

    .nav-item:hover {
        background: rgba(255,255,255,0.05);
    }

    .nav-item.active {
        background: rgba(52, 152, 219, 0.2);
        border-left-color: #3498db;
        color: #3498db;
        font-weight: 500;
    }

    .icon { margin-right: 10px; min-width: 25px; }
</style>
<nav class="bottom-nav">
    <div class="bottom-nav__slot bottom-nav__slot--left">
        <UserMenu/>
    </div>

    <button class="bottom-nav__logo" @onclick="GoHome">
        <SvgIcon Name="logos-logo-icon"/>
    </button>

    <div class="bottom-nav__slot bottom-nav__slot--right">
        <button class="hamburger hamburger--inline @(IsMenuOpen ? "hamburger--active" : "")"
                @onclick="ToggleMenu">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </button>
    </div>
</nav>
